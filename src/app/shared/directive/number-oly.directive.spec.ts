import { NumberOlyDirective } from './number-oly.directive';

describe('NumberOlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberOlyDirective();
    expect(directive).toBeTruthy();
  });
});
